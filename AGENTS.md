# Repository Instructions

## Coding conventions
- Use Python 3.11+ with full type hints on public functions and methods.
- Keep functions small, side effects explicit, and module boundaries clear.
- Run formatting/lint checks with `ruff` and static checks with `mypy`.

## Workflow
- Run tests before finishing any task.
- Keep diffs small and focused.
- Explain changes briefly in summaries and PR descriptions.
